# Marketing Cloud 客户经营一期 PRD

## 1. 产品定位

Marketing Cloud 不是替代现有营销平台，而是在现有官网、活动、内容、用户分组、CRM 同步等能力上增加一层客户经营辅助能力。

一期核心链路：

```text
客户行为数据
-> 规则识别意向
-> AI 解释原因
-> AI 推荐经营动作
-> 运营人工采纳执行
-> 回收动作结果
```

核心问题：

```text
ToB 转化周期长，市场人员不知道哪些客户值得经营、哪些企业正在升温、下一步应该做什么。
```

一期目标：

```text
把市场人员从“手工筛客户”变成“处理系统推荐的客户经营任务”。
```

## 2. 一期页面范围

当前原型按以下三级信息架构组织。详情页和新建页不作为常驻菜单。

```text
客户经营
├─ 看板
├─ 意向分层
├─ 企业账户
│  └─ 查看洞察 -> 企业洞察（详情页）
└─ 动作中心

用户中心
├─ 用户列表
│  └─ 查看画像 -> 用户画像（详情页）
├─ 用户分组
│  └─ AI 创建分组（创建页）
└─ 用户画像

系统设置
├─ 客户意向评分规则
├─ 行为权重
├─ 权限
└─ 规则版本
```

说明：

```text
AI 分组不单独成为一套新体系，而是作为“智能创建方式”接入现有用户分组。
```

## 3. 一期边界

### 3.1 一期做

- 客户意向评分
- 企业意向评分
- 客户意向分层
- 企业账户经营化改造
- 企业洞察
- 经营看板
- 渠道转化和转化入口统计
- 推荐动作列表和推荐动作中心
- 用户画像与系统自动标签
- AI 洞察解释
- AI 创建用户分组
- CRM 同步状态展示和同步动作
- 销售提醒、标记企业机会、加入分组等轻动作

### 3.2 一期不做

- 复杂机器学习模型
- 全自动 AI Agent
- 自动创建销售跟进任务
- 自动创建 CRM 商机
- 自动创建完整 Journey
- 自动生成完整营销方案
- 复杂多触点归因
- 高级规则审批流、复杂版本灰度和自动化发布

## 4. 数据口径

### 4.1 渠道和转化入口

渠道回答：

```text
客户从哪里来？
```

转化入口回答：

```text
客户在哪里留下可经营信息？
```

两者不能混在同一个维度里。例如：

```text
自然搜索进入官网 -> 下载资料并留资

渠道 = 自然搜索
转化入口 = 资料下载
```

### 4.2 渠道转化

建议渠道：

- 自然搜索
- 直接访问
- 付费广告
- 活动渠道
- 内容渠道
- 企微分享
- 外部链接

字段：

```text
渠道
访问/触达
高意向
可经营客户
同步 CRM
销售机会
```

其中“可经营客户”定义为：

```text
已获得明确身份或有效联系方式，可继续触达或同步 CRM 的客户。
```

### 4.3 转化入口

建议入口：

- 官网表单
- 落地页表单
- 活动报名
- 资料下载
- 咨询表单
- 企微添加

字段：

```text
转化入口
留资人数
有效留资
同步 CRM
销售机会
转化率
```

活动报名的口径：

```text
活动报名成功且提交有效联系方式 = 活动场景下的有效留资。
```

### 4.4 沉默用户和无效用户

沉默用户不是脏数据，应保留在客户池中。

沉默用户定义：

```text
曾经有过行为或留资，但最近 30/60/90 天无有效活跃。
```

处理方式：

- 保留客户档案
- 意向分随时间衰减
- 进入沉默客户分层
- 可加入召回分组
- 不默认推给销售

无效用户是数据质量问题，应从经营分析和推荐动作中排除。

无效用户定义：

- 手机号无效：大陆手机号11位，13011112222等人为编造的；发送短信返回手机号为空号；
- 测试数据
- 重复数据
- 竞品/学生

处理方式：

- 打上无效标记
- 从意向评分中排除
- 从推荐动作中排除
- 从转化率统计中剔除或单独展示
- 保留原始记录用于审计

建议字段：

```text
user_status: active / silent / invalid
invalid_reason
silent_since
is_excluded_from_scoring
is_excluded_from_recommendation
```

## 5. 客户意向评分

客户意向分范围为 0 到 100 分。一期采用规则模型，AI 只做解释和推荐，不直接决定分层。

```text
客户意向分 = 行为分 + 内容分 + 活动分 + CRM状态分
```

每条行为需要乘以时间衰减系数：

```text
单条行为得分 = 行为基础分 × 时间衰减系数
```

最终：

```text
客户意向分 = 所有行为得分累加，最高封顶 100 分
```

### 5.1 行为分

| 行为 | 分值 |
| --- | ---: |
| 访问官网/落地页 | +2 |
| 访问产品页 | +5 |
| 访问价格/方案页 | +10 |
| 下载资料 | +8 |
| 提交表单 | +20 |
| 重复访问同一内容 | +5 |

### 5.2 内容分

| 内容类型 | 分值 |
| --- | ---: |
| 阅读新闻资讯 | +2 |
| 阅读行业文章 | +5 |
| 阅读产品介绍 | +8 |
| 阅读客户案例 | +10 |
| 阅读解决方案 | +12 |
| 下载白皮书 | +15 |

### 5.3 活动分

| 活动行为 | 分值 |
| --- | ---: |
| 报名活动 | +15 |
| 实际签到 | +25 |
| 观看直播超过 10 分钟 | +15 |
| 参与问卷/互动 | +10 |
| 活动后访问产品页 | +15 |

### 5.4 时间衰减

| 行为时间 | 计分比例 |
| --- | ---: |
| 7 天内 | 100% |
| 8 到 30 天 | 70% |
| 31 到 90 天 | 40% |
| 90 天以上 | 20% |

建议默认只统计近 180 天行为，避免历史行为长期抬高分数。

### 5.5 转化入口与 CRM 状态

官网表单、落地页表单、活动报名、资料下载、咨询表单等转化入口必须进入意向评分明细，即使暂不支持内容元数据增强，也不能被行为计算链路过滤。

建议至少保留：

```text
conversion_entry
conversion_time
is_valid_contact
crm_sync_status
crm_customer_stage
```

CRM 状态用于经营解释和推荐动作，不直接覆盖行为得分。例如：

```text
高意向 + 未同步 CRM -> 推荐同步 CRM
高意向 + 已同步 CRM -> 提醒销售关注
已有客户 + 近期高价值行为 -> 推荐增购/复购经营
```

### 5.6 客户意向等级

| 等级 | 分数 | 运营含义 |
| --- | ---: | --- |
| 高意向 | 80 到 100 | 优先经营或销售提醒 |
| 中意向 | 50 到 79 | 持续培育 |
| 低意向 | 20 到 49 | 内容触达 |
| 沉默客户 | 0 到 19 | 召回或暂缓 |

注意：

```text
已留资不一定等于高意向。更稳的判断是“留资 + 高价值行为”。
```

## 6. 用户画像与标签

一期用户画像与标签建议基于现有 MK 用户标签画像计算引擎实现，不重新设计一套标签体系。原型层做业务化呈现，研发层复用现有接口：

```text
GET /api/v1/tag/user/{yhtId}/profile
```

### 6.1 一期原则

```text
现有标签引擎 = 底层能力
画像标签页面 = 运营可理解的展示层
客户经营评分 = 独立读取行为、转化入口和 CRM 数据计算
```

一期不重新做复杂标签平台，不开放完整标签规则配置后台。AI 不直接生成不可控标签，只负责解释画像、推荐动作和辅助分组。

验收口径：

```text
客户经营相关页面按一期 PRD 的 intent_score、account_score、推荐动作规则验收；
README 中的标签、RFM 和历史兼容字段作为画像底座，不直接替代一期意向分和客户分层。
```

### 6.2 现有接口结构

| 字段 | 含义 | 一期展示方式 |
| --- | --- | --- |
| yht_id | 统一用户 ID | 用户画像摘要 |
| generated_at | 画像生成时间 | 更新时间 |
| fact_tags | 事实标签 | 基础画像字段 |
| behavior_tags | 行为标签分组 | 产品/行业/领域/角色/场景等兴趣 |
| interest_tags | 兴趣标签 | 内容推荐、分组圈选 |
| lifecycle | README 历史技术字段 | 产品层不展示，不作为经营阶段 |
| rfm | 行为参考指标 | 最近活跃、命中次数、行为均分 |
| active_score | 历史综合标签分 | 仅用于技术诊断，不用于经营判断 |
| lead_grade | 历史评级兼容字段 | 不在经营页面作为主字段展示 |

一期需要新增业务字段：

| 字段 | 说明 | 用途 |
| --- | --- | --- |
| user_scope | internal_employee / customer / partner / unknown | 区分员工、客户、伙伴 |
| crm_customer_stage | CRM 客户阶段 | 销售/客户状态 |
| intent_score | 一期客户经营意向分 | 客户分层、推荐动作 |
| intent_level | high / medium / low / silent | 经营页面统一分层 |
| behavior_active_score | 只基于行为数据计算的活跃参考分 | 画像辅助解释 |
| exclude_reason | 排除原因 | 员工、无效用户、测试数据说明 |
| is_excluded_from_scoring | 是否排除意向评分 | 控制是否生成 intent_score |
| is_excluded_from_recommendation | 是否排除推荐动作 | 员工、无效用户、测试数据排除 |

一期建议画像接口业务响应：

```json
{
  "user_scope": "internal_employee",
  "last_active_time": "2026-06-02T08:30:00+08:00",
  "behavior_count_7d": 3,
  "behavior_count_30d": 18,
  "behavior_active_score": 42,
  "intent_score": null,
  "intent_level": null,
  "is_excluded_from_scoring": true,
  "is_excluded_from_recommendation": true,
  "exclude_reason": "内部员工"
}
```

### 6.3 标签体系映射

现有 README 中已有 5 类标签：

| 标签类别 | 现有含义 | 一期原型展示 |
| --- | --- | --- |
| 事实标签 | member 用户属性，不衰减 | 身份、公司、部门、职位、地区、注册来源、注册类型 |
| 行为标签 | 三层结构，大类到内容 | 产品、行业、领域、角色、场景、规模、身份、组织等 Top 标签 |
| 兴趣标签 | 行为标签聚合 | 数字营销、财务等兴趣强度 |
| 规则标签 | 逻辑判定型 | 后续可承接高活跃、深度浏览等 |
| README lifecycle 标签 | 历史状态机模型 | 一期产品层不展示，可保留为技术兼容数据 |

事实标签字段映射：

| member 字段 | 标签示例 | 说明 |
| --- | --- | --- |
| is_employee | FACT_EMPLOYEE_1 | 员工/非员工 |
| company | FACT_COMPANY_用友网络 | 公司 |
| department | FACT_DEPT_集团数智化部 | 部门 |
| position | FACT_POSITION_产品总监 | 职位 |
| district | FACT_DISTRICT_北京 | 地区 |
| register_from | FACT_REG_FROM_官网 | 注册来源 |
| register_type | FACT_REG_TYPE_手机号 | 注册类型 |

### 6.4 行为活跃指标

一期不新增“行为生命周期”或“行为活跃状态”作为业务分层，画像页只展示客观行为指标：

```text
最近活跃时间
近 7/30 天行为次数
行为命中总次数
行为标签平均分
```

这些指标只用于解释行为新鲜度、支持分组筛选和推荐规则，不替代客户意向分层，也不代表 CRM 客户阶段。

如需要输出活跃参考分，必须只统计行为类数据：

```text
behavior_active_score = 基于行为频次、最近活跃时间和行为标签分值计算
```

不得将公司、职位、员工身份等事实标签纳入行为活跃分。原有 `active_score` 可保留为技术诊断指标，建议对外改名为 `tag_health_score`。

### 6.5 RFM 与历史评级兼容

RFM 字段：

| 字段 | 含义 |
| --- | --- |
| recency_days | 最近活跃天数 |
| total_hits | 行为命中总次数 |
| avg_score | 行为标签平均分 |

行为活跃参考分：

```text
behavior_active_score = 仅基于行为标签和最近活跃时间计算
```

`lead_grade` 可作为存量接口兼容字段保留，但经营页面、看板统计、推荐动作和用户分组统一使用：

```text
intent_score
intent_level
```

不再同时向业务用户展示两套高/中/低评级，避免重复判断。

### 6.6 当前接口数据优化建议

基于 `用户画像接口数据.json`，建议优化：

1. 员工数据要排除客户经营推荐。

当前样本事实标签为员工：

```text
FACT_EMPLOYEE_1
```

建议增加：

```text
user_scope: internal_employee / customer / partner / unknown
```

内部员工默认不进入客户推荐动作和客户转化统计。

规则：

```text
user_scope = internal_employee
-> 不进入经营看板客户转化统计
-> 不进入客户意向分层
-> 不生成推荐动作
-> 可保留在画像标签页面用于数据审计
```

2. 同组行为标签存在重复值。

例如：

```text
产品：用友BIP 重复出现
规模：大型企业 重复出现
```

建议同组内按 value 合并：

```text
score 合并或取最大值
hits 累加
last_hit 取最新
```

3. 标签命中多但热度低，需要业务解释。

样本：

```text
total_hits = 197
avg_score = 6
active_score = 12
```

说明历史行为多，但标签分值已衰减或新鲜度不足。界面需要提示：

```text
历史行为丰富，但近期经营热度偏低。
```

4. README lifecycle 不进入产品业务语义。

`lifecycle` 可作为历史技术字段保留，但画像页和经营页面不展示生命周期阶段，也不再映射为新的业务分层。需要表达活跃度时，直接展示“最近活跃时间、近 7/30 天行为次数”等客观数据。

5. lead_grade 降为兼容字段。

```text
lead_grade = 存量接口兼容
intent_score + intent_level = 客户经营统一判断
```

经营页面不同时展示两套评级。

6. 企业账户需要增加用户级画像聚合。

README 当前主要是单用户画像。企业账户洞察需要额外聚合企业下联系人：

```text
企业下联系人画像
-> 排除 internal_employee / invalid 用户
-> 识别关键角色
-> 聚合 intent_score、关键角色、兴趣标签、近期高价值行为
-> 参与 account_score 和 AI 判断
```

7. 转化入口行为必须参与意向评分。

`form`、`applyform`、活动报名、资料下载等行为即使没有内容增强结果，也必须进入转化入口统计和 `intent_score` 评分明细，不能在数据摄入或身份对齐阶段被整体过滤。

### 6.7 标签和用户分组关系

```text
标签 = 用户身上的状态或特征
分组 = 按条件圈出的一批用户
```

用户分组可以引用事实标签、行为标签、兴趣标签、最近活跃时间、intent_score 和 intent_level 作为条件。`lead_grade` 仅用于兼容存量分组。

## 7. 企业意向评分

企业意向分由规则模型计算，AI 判断基于评分明细、企业画像、行为数据和 CRM 状态生成解释与推荐动作。

企业意向分满分 100 分：

```text
企业意向分 =
联系人意向分
+ 多人活跃加分
+ 关键角色加分
+ 高价值行为加分
+ 企业画像/历史价值加分
```

### 7.1 评分因子

| 因子 | 上限 | 说明 |
| --- | ---: | --- |
| 联系人意向分 | 40 | 企业下最近活跃联系人意向分加权 |
| 多人活跃加分 | 20 | 同企业多人近期活跃 |
| 关键角色加分 | 15 | 决策人、负责人、采购、IT 等关键角色活跃 |
| 高价值行为加分 | 15 | 访问方案页、价格页、下载资料、报名活动 |
| 企业画像/历史价值加分 | 10 | 规模、营收、历史签约、在用产品等 |

联系人意向分建议：

```text
联系人意向分 = Top 3 活跃联系人意向分均值 × 40%
```

多人活跃加分：

| 最近 7 天同企业活跃联系人 | 加分 |
| --- | ---: |
| 1 人 | 0 |
| 2 人 | +8 |
| 3 到 5 人 | +15 |
| 5 人以上 | +20 |

关键角色加分：

| 条件 | 加分 |
| --- | ---: |
| 最近 30 天有 1 名关键角色活跃 | +8 |
| 最近 30 天有 2 名及以上关键角色活跃 | +15 |

企业画像/历史价值加分：

| 条件 | 加分 |
| --- | ---: |
| 中大型企业 | +3 |
| 行业匹配目标行业 | +2 |
| 历史签约客户 | +3 |
| 当前产品使用中 | +2 |

### 7.2 企业状态

| 企业状态 | 分数 | 说明 |
| --- | ---: | --- |
| 高意向企业 | 80 到 100 | 建议标记企业机会 |
| 升温企业 | 60 到 79 | 近期关注，持续观察 |
| 培育企业 | 40 到 59 | 内容或活动培育 |
| 低活跃企业 | 0 到 39 | 暂缓或召回 |

### 7.3 企业意向变化

一期不做完整趋势图，展示：

```text
当前意向分
近7天变化
近30天变化
升温/下降状态
近期升温原因
```

前提数据：

```text
account_id
intent_score
intent_level
score_detail
calculated_time
rule_version
```

## 8. AI 经营洞察

AI 洞察采用：

```text
规则触发 + AI 生成解释 + AI 推荐动作
```

AI 不直接决定客户分层或企业分层。

一期建议洞察类别：

| 洞察类别 | 触发示例 | 推荐动作 |
| --- | --- | --- |
| 官网留资/CRM 同步 | 官网表单留资已自动同步 CRM | 提醒销售关注 |
| 高意向未留资 | 意向分 >= 80 且未留资 | 加入再营销分组 |
| 活动后转化 | 签到后访问方案页或产品页 | 活动回访 |
| 企业多人活跃 | 同企业 3 人以上活跃 | 标记企业机会 |
| 销售未跟进预警 | 已同步 CRM 超过 24 小时未跟进 | 提醒销售 |
| 内容效果洞察 | 某内容带来高意向客户 | 推荐相关内容 |
| 沉默/流失预警 | 曾经高意向但 30 天无活跃 | 召回分组 |

AI 输出建议结构：

```text
判断结论
原因解释
风险提醒
推荐动作
```

## 9. 推荐动作

一期推荐动作必须是当前系统可执行的动作。

不做：

```text
创建销售跟进任务
创建 CRM 商机
```

一期推荐动作：

| 推荐动作 | 适用场景 | 业务意义 |
| --- | --- | --- |
| 同步 CRM | 已留资、有效联系方式、未进 CRM | 交付销售线索 |
| 提醒销售跟进 | 已同步 CRM，且近期高价值行为 | 推动销售及时关注 |
| 标记企业机会 | 企业多人活跃或企业意向分高 | 从个人线索升级到企业经营 |
| 加入再营销分组 | 高意向但未留资 | 继续培育和触达 |
| 发送推荐内容 | 中意向客户阅读过案例/方案 | 降低直接销售打扰 |
| 活动回访 | 报名未签到或活动后升温 | 承接活动热度 |
| 召回沉默客户 | 曾经高意向但长期无活跃 | 防止潜在客户流失 |
| 标记无效线索 | 测试、重复、无效联系方式 | 提升数据质量 |

推荐动作状态：

```text
待处理 -> 已采纳 -> 已执行 -> 已完成 -> 已忽略
```

字段：

```text
推荐类型
客户/企业
优先级
推荐原因
建议动作
预计收益
负责人
状态
```

## 10. 页面说明

### 10.1 AI 经营看板

页面目标：

```text
让市场负责人快速看到今日经营动作、近 7 天渠道/入口转化、企业升温摘要和待处理推荐动作。
```

顶部时间筛选：

```text
今日 / 昨日 / 近7天 / 近30天 / 自定义
```

默认口径：

| 模块 | 默认口径 |
| --- | --- |
| 顶部指标 | 今日 |
| AI 洞察 | 近 7 天 |
| 渠道/入口转化 | 近 7 天 |
| 推荐动作列表 | 待处理 + 最近 7 天触发 |
| 企业经营摘要 | 近 7 天企业升温与历史客户再活跃 |
| 运营资源概览 | 当前累计状态 |

顶部指标：

- 新增留资客户
- 已同步 CRM 线索
- 待经营高意向客户
- 活动转化客户

看板不展示客户经营漏斗。一期先避免口径复杂。

### 10.2 客户意向分层

页面目标：

```text
让运营按高/中/低/沉默分层处理客户。
```

筛选条件：

- 行业
- 地区
- 客户类型
- 意向等级
- 最近活跃时间
- 行为类型
- 是否已同步 CRM

列表字段：

- 客户名称
- 所属企业
- 行业
- 意向分
- 最近行为
- CRM 状态
- 推荐动作

### 10.3 企业账户

页面目标：

```text
把公司信息列表从资料台账升级为企业账户经营入口。
```

字段：

```text
公司名称
企业意向分
企业状态
近7天变化
用户人数
参与活动人数
在用产品
最近签约
累计签约
行业
CRM 状态
推荐动作
```

不展示所属销售。

筛选：

- 公司名称/行业/产品关键词
- 企业状态
- CRM 状态
- 高级筛选

操作：

- 标记机会
- 同步 CRM
- 发送内容
- 加入分组
- 查看洞察

交互规则：

```text
企业账户列表 -> 查看洞察 -> 企业洞察详情页
```

企业洞察不是左侧常驻菜单，避免与企业账户列表产生重复入口。

### 10.4 企业洞察

页面目标：

```text
从个人线索升级为企业账户经营，判断企业是否值得关注、是否升温、是否适合提醒销售。
```

模块：

- 企业基础画像
- 互动分析
- 签约频率分析
- 产品使用状态
- 购买记录
- 企业下用户级画像
- 企业意向变化
- AI 判断
- 活跃联系人
- 关键行为时间线

企业基础画像字段：

- 企业规模
- 人员规模
- 注册资本
- 成立时间
- 行业
- 营业收入
- 在用产品

企业意向变化字段：

- 当前意向分
- 近 7 天变化
- 近 30 天变化
- 升温/下降状态
- 近期升温原因

AI 判断：

```text
基于企业意向分、联系人活跃、关键角色、高价值行为、企业画像、历史签约和产品使用状态生成解释与动作建议。
```

企业下用户级画像字段：

```text
联系人
身份：客户 / 伙伴 / 内部员工 / unknown
职位/角色
intent_score / intent_level
最近活跃时间
近期高价值行为
兴趣标签
经营处理：纳入评分 / 优先提醒 / 排除推荐
```

处理规则：

```text
internal_employee 和 invalid 用户不参与企业经营推荐；
客户和伙伴可参与企业意向分聚合；
关键角色、高意向联系人、近期高价值行为用于提升企业机会优先级。
```

### 10.5 推荐动作中心

页面目标：

```text
把 AI 推荐转成可执行、可跟踪的运营动作。
```

列表字段：

- 推荐类型
- 客户/企业
- 优先级
- 推荐原因
- 建议动作
- 预计收益
- 负责人
- 状态

### 10.6 用户列表

页面目标：

```text
统一管理用户基础信息、身份、标签、经营资格和最近活跃状态；客户意向判断仍以“意向分层”为经营入口。
```

列表字段：

- 姓名
- 所属企业
- 身份
- 职位
- 用户状态：可经营 / 已排除
- 最近活跃
- 标签
- 操作：查看画像

交互规则：

```text
用户列表 / 意向分层客户姓名 -> 查看画像 -> 用户画像详情页
```

### 10.7 用户画像

页面目标：

```text
以单个用户为中心，说明“这个人是谁、关注什么、近期做了什么、是否具备经营资格”。
```

模块：

- 用户画像摘要
- 事实标签
- 行为标签 Top 分组
- 兴趣标签
- 最近活跃时间 / 行为参考指标
- 经营资格与排除原因
- 客户意向分 / 意向等级
- 当前接口数据优化建议

内部员工和无效用户可以保留画像数据，但客户意向分为空，不生成经营动作。

核心说明：

```text
底层按现有 MK 标签画像引擎实现，原型只做业务化呈现。画像描述用户特征，`intent_score + intent_level` 负责经营判断；AI 不直接生成不可控标签，主要用于解释画像和辅助推荐动作。
```

### 10.8 用户分组

页面目标：

```text
保留现有用户分组体系，并新增 AI 创建分组能力。
```

原则：

```text
AI 创建分组不是新体系，而是现有用户分组的智能创建方式。
```

分组字段：

- 分组名称
- 人数
- 类型：固定组 / 条件组
- 创建方式：手动创建 / AI 生成
- 更新方式：不自动更新 / 每日自动 / 每周自动
- 简介
- 创建时间
- 操作

### 10.9 AI 创建分组

页面目标：

```text
通过自然语言生成条件组规则，确认 CRM 经营状态和创建后动作后，保存到现有用户分组。手动创建分组继续沿用现有 MK 条件组创建流程，不复用 AI 创建页面。
```

流程：

```text
自然语言输入
-> AI 解析与待确认项
-> 分组定义与可编辑条件
-> CRM 状态分流与样本名单
-> 配置创建后动作
-> 保存为用户分组并发布
```

示例：

```text
帮我找出最近30天活跃下降，但曾经参加过活动的制造业客户
```

解析结果：

- 行业 = 制造业
- 最近 30 天活跃下降
- 历史参加过活动
- 意向分 > 40

新建分组字段：

- 分组名称
- 分组类型：固定组 / 条件组
- 创建方式：手动创建 / AI 生成
- 更新频率
- 负责人
- 分组说明
- 圈选条件
- 样本名单
- 创建后动作

AI 创建分组新增模块：

- 常用模板：高意向未进入 CRM、方案页高频访问未留资、活动后沉默客户召回、活动报名未签到、多人活跃企业、沉默高意向客户
- AI 待确认项：例如“参加活动”是否包含报名未签到，由运营选择“报名或签到”或“仅签到”
- CRM 经营状态：全部客户、未进入 CRM、销售跟进中、暂时无效、已转化
- CRM 分流原则：不重复创建线索；跟进中的客户回写行为并按阈值提醒销售；已转化客户更新 Contact/Account 动态；暂时无效客户仅在满足再激活门槛后进入复核
- 创建后动作：内容召回包、创建新线索、更新既有线索、再激活待确认、更新已转化客户动态、建立效果追踪

### 10.10 系统设置

一期提供基础可配置、可追溯的规则管理入口，不支持复杂审批流。

| 页面 | 一期能力 |
| --- | --- |
| 客户意向评分规则 | 查看评分组成、分层阈值、企业加成和当前生效版本 |
| 行为权重 | 配置行为/内容/活动事件的基础分、计分限制、启停状态和时间衰减 |
| 权限 | 按市场运营、销售运营、系统管理员控制数据查看和操作范围 |
| 规则版本 | 记录发布、生效、待发布与回滚依据 |

行为权重配置一期口径：

```text
评分模式 = 规则加分制
单条行为得分 = 基础分 × 时间衰减系数
客户意向分 = 有效行为得分累加，最高 100 分
统计窗口 = 最近 180 天
```

研发配置中的 `weight_bv / weight_journey / weight_conv` 三段权重、`funnel_level` 漏斗层级和 `conversion_score` 转化分不进入一期经营评分页面。原因是它们与一期规则加分制、暂不展示经营漏斗及转化入口独立统计的口径存在冲突。如后续启用，需单独评审评分公式、数据来源和防止重复计分的规则。

版本发布规则：

- 保存仅生成草稿，不影响当前客户分数
- 发布前校验事件编码唯一、分值范围、计分上限和时间衰减配置
- 发布生成不可变版本快照，并提交异步重算任务
- 重算完成前继续使用上一生效版本，任务失败不切换版本
- 历史版本保留，可通过再次发布历史快照实现回滚

## 11. 数据表建议

### 11.1 客户行为表

```text
user_id
account_id
event_type
event_name
event_time
content_id
activity_id
channel
conversion_entry
properties
```

### 11.2 客户画像表

```text
user_id
account_id
user_scope
industry
region
position
role_type
source
last_active_time
behavior_active_score
crm_customer_stage
intent_score
intent_level
crm_status
user_status
invalid_reason
silent_since
is_excluded_from_scoring
is_excluded_from_recommendation
exclude_reason
owner
```

### 11.3 留资记录表

```text
conversion_id
user_id
account_id
conversion_entry
channel
form_type
contact_phone
contact_email
is_valid_contact
is_duplicate
conversion_time
crm_sync_status
crm_sync_time
```

### 11.4 企业账户表

```text
account_id
account_name
industry
sub_industry
region
company_scale
employee_size
registered_capital
annual_revenue
established_year
crm_status
current_products
last_contract_date
contract_count
conversion_cycle_days
```

### 11.5 意向评分表

```text
target_id
target_type: user/account
intent_score
intent_level
score_detail
rule_version
last_calculated_time
```

### 11.6 推荐动作表

```text
recommendation_id
target_id
target_type
recommendation_type
priority
reason
suggested_action
status
owner
created_time
updated_time
result
```

### 11.7 用户标签定义表

```text
tag_id
tag_code
tag_name
tag_type
rule_detail
rule_version
update_mode
status
created_time
updated_time
```

### 11.8 用户标签关系表

```text
user_id
tag_code
tag_value
generated_by: system/manual/ai
rule_version
generated_time
expired_time
```

### 11.9 用户分组表

```text
group_id
group_name
group_type: fixed/condition
create_method: manual/ai
update_mode
rule_detail
estimated_count
description
owner
created_time
updated_time
```

## 12. 研发实施顺序

建议分 7 步：

1. 打通基础数据：行为、留资、活动、资料、CRM、客户画像、企业画像
2. 实现基础用户画像和系统自动标签
3. 实现客户意向评分和分层
4. 实现企业意向评分、公司列表和企业账户洞察
5. 实现经营看板和渠道/入口转化
6. 实现推荐动作中心和动作状态流转
7. 实现 AI 创建用户分组

## 13. 和研发沟通口径

一期不是做“AI 自动营销”，而是做：

```text
规则识别客户/企业意向
+ AI 解释原因
+ AI 推荐下一步动作
+ 人工采纳执行
```

关键原则：

- 分层和评分由规则模型计算，AI 不直接决定
- AI 负责解释原因、总结机会、推荐动作
- 用户画像和系统标签是一期开工前置底座
- 推荐动作必须是当前系统可执行的轻动作
- AI 分组必须接入现有用户分组体系
- 企业账户和企业洞察服务于 ToB Account 经营
- 一期先跑通闭环，沉淀数据后再升级模型和规则配置
