# 客户表达 V2.1

以 customer-expression-prd-v2.1.md 和同版 Word 为当前开发基线。

原型入口：`/mk/#profileTags` 与 `/mk/#customerProfile`。

本次调整：保留 YonWork 读取与原有实时＋定时线索链路；自动校验后异步更新 CRM 线索客户表达字段；无逐条人工审核；可信度、洞察、建议与反馈后置。两个画像入口共用表达记录和原始问答窗口。

原型为静态演示，状态切换不会调用真实 AI 或 CRM。默认三条表达，查看全部展示四条示例；生产分页、鉴权、任务处理按 PRD 实现。反馈入口已移除，后续再增加。

验证：Word 七页渲染检查；JavaScript 语法检查；Git diff 空白检查。本次未执行浏览器交互测试。
