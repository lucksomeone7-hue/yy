# README 与一期原型分歧点及研发改造清单

> 原型同步版本：2026-08-10。本文已同步最新动作术语、企业聚合去重和经营动作中心状态口径。

## 1. 总体结论

README 描述的是 MK 用户标签画像计算引擎，属于底层能力。一期原型和 PRD 描述的是 AI 客户经营工作台，属于业务应用层。

最终口径：

```text
底层画像能力参考 README
客户经营页面和验收规则按一期 PRD
```

## 2. 分歧点

| 分歧点 | README 当前口径 | 一期原型/PRD 口径 | 最终处理 |
| --- | --- | --- | --- |
| 产品定位 | 用户标签与画像计算引擎 | AI 客户经营工作台 | README 作为底层能力，一期做业务应用层 |
| 客户评分 | lead_grade 或综合评级 | intent_score: 0-100 | 经营页面统一使用 intent_score + intent_level，lead_grade 仅兼容 |
| 高意向定义 | 综合评级达到阈值 | 高意向客户 intent_score >= 80 | 高意向按一期规则，不再并列展示两套评级 |
| README lifecycle | 首次接触/初步了解/持续关注/关系沉寂 | 一期不需要生命周期业务分层 | 产品层不展示 lifecycle，直接展示最近活跃等客观指标 |
| 老客户含义 | 超过 14 天未活跃可能为老客户 | 老客户通常是 CRM/历史成交客户 | 不直接使用“老客户”作为经营阶段 |
| 标签分值 | 标签分值有衰减 | 意向分按行为、内容、活动、转化入口、CRM 状态计算 | 标签分不能直接等于意向分 |
| 员工数据 | fact_tags 中可出现员工 | 客户经营面向外部客户/企业 | 增加 user_scope，员工排除经营推荐 |
| 用户级画像 | README 已有单用户画像 | 企业账户洞察需要 Account 聚合 | 企业页新增“企业下用户级画像” |
| 企业账户 | README 主要是用户级 | 一期需要企业意向分、公司列表、企业洞察 | 新增企业聚合评分和公司经营列表 |
| 建议与经营动作 | README 偏评分/预测 API | 下一步建议、销售交接建议、经营动作中心 | 业务层生成建议，采纳后创建经营动作并统一追踪 |
| 用户分组 | README 支持按标签查用户 | AI 创建分组接入现有用户分组 | AI 分组作为现有分组的创建方式 |
| 商机预测 | README 有商机激增/流失风险 | 一期暂不做复杂预测 | 放二期，一期只做规则洞察 |

## 3. 需要调整的现有能力

### 3.1 用户身份字段

现状：

```text
fact_tags 中有 FACT_EMPLOYEE_1，但没有业务级身份字段。
```

需要新增：

```text
user_scope: internal_employee / customer / partner / unknown
```

规则：

```text
internal_employee 不进入经营看板客户转化统计
internal_employee 不进入客户意向分层
internal_employee 不生成推荐动作
internal_employee 可保留在画像标签页用于审计
```

### 3.2 README lifecycle 处理

现状：

```text
lifecycle.stage = 老客户
```

调整：

```text
产品层不展示 lifecycle 原始术语
不再新建“行为生命周期”或“行为活跃状态”业务分层
需要表达活跃程度时，展示最近活跃时间和近 7/30 天行为次数
```

建议使用：

```text
last_active_time
behavior_count_7d
behavior_count_30d
behavior_active_score
crm_customer_stage
```

`behavior_active_score` 仅统计行为数据；`lifecycle` 可保留为底层历史兼容字段，但不得驱动客户意向分层。

### 3.3 标签去重合并

现状样例：

```text
产品：用友BIP 重复出现
规模：大型企业 重复出现
```

调整：

```text
同一 behavior_tags 分组内按 value 合并
hits 累加
score 取最大值或按规则合并
last_hit 取最新
```

### 3.4 lead_grade 与 intent_score 收敛

现状：

```text
README 输出 lead_grade = A/B/C
```

一期新增：

```text
intent_score = 0-100
intent_level = 高意向 / 中意向 / 低意向 / 沉默
```

处理：

```text
lead_grade 仅用于存量接口和存量分组兼容
intent_score + intent_level 用于客户分层、推荐动作、看板统计
经营页面不同时展示两套评级
```

### 3.5 员工/无效用户排除

需要新增排除字段：

```text
is_excluded_from_scoring
is_excluded_from_recommendation
invalid_reason
exclude_reason
```

排除对象：

```text
内部员工
测试数据
无效手机号/邮箱
重复数据
竞品/学生/供应商
非目标客户
```

## 4. 需要新增的业务层能力

### 4.1 客户经营意向分

新增：

```text
intent_score
intent_level
score_detail
rule_version
last_calculated_time
```

客户分层：

```text
80-100 高意向
50-79 中意向
20-49 低意向
0-19 沉默客户
```

### 4.2 企业意向分

新增：

```text
account_score
account_level
account_score_detail
```

企业评分需聚合：

```text
企业下联系人 intent_score
关键角色
多人活跃
高价值行为
企业画像/历史签约
产品使用状态
```

### 4.3 企业下用户级画像

企业账户洞察需新增联系人画像列表：

```text
联系人
身份
职位/角色
intent_score / intent_level
最近活跃时间
近期高价值行为
兴趣标签
经营处理
```

处理规则：

```text
internal_employee 排除企业经营推荐
customer / partner 可参与企业意向聚合
关键角色和高价值行为提升企业机会优先级
```

### 4.4 建议与经营动作业务层

建议由业务层规则生成，不直接来自 README；用户采纳后才创建经营动作。

页面口径：

```text
意向分层：下一步建议
企业账户：销售交接建议
动作执行与反馈：经营动作中心
```

一期动作：

```text
同步 CRM
更新 CRM 动态
提醒销售跟进
标记企业机会
加入再营销分组
内容培育
活动回访
召回沉默客户
标记无效线索
```

企业账户不直接发送内容或加入分组，只处理 CRM 同步、CRM 动态更新和企业机会信号。同企业多人触发同类动作时按 `aggregation_key` 合并，通过同一 `recommendation_id/action_id` 在各页面展示和更新状态。

不做：

```text
自动创建销售跟进任务
自动创建 CRM 商机
```

### 4.5 AI 分组接入现有用户分组

AI 分组不是新体系。

流程：

```text
自然语言输入
-> AI 解析条件
-> 回填现有条件组
-> 保存为用户分组
```

分组表需保留：

```text
group_type: fixed / condition
create_method: manual / ai
rule_detail
```

## 5. 页面需要调整的位置

| 页面 | 需要调整/新增 |
| --- | --- |
| 画像标签 | 增加 user_scope、排除状态；不展示 lifecycle；活跃指标只统计行为 |
| 客户意向分层 | 统一按 intent_score + intent_level 分层，不并列展示 lead_grade |
| 经营看板 | 排除 internal_employee 和 invalid 用户 |
| 公司列表 | 使用 account_score 和企业状态，不展示所属销售 |
| 企业账户洞察 | 增加企业下用户级画像，员工不参与推荐 |
| 经营动作中心 | 建议按业务规则生成；联系人、企业、人群动作统一去重和流转 |
| 用户分组 | 支持引用标签、最近活跃、intent_score、intent_level；lead_grade 仅兼容存量 |

## 6. 研发建议顺序

1. 在现有画像接口上补 `user_scope` 和排除字段。
2. 对 behavior_tags 做同组 value 去重合并。
3. 停止在产品层展示 lifecycle，补最近活跃和行为活跃参考指标。
4. 将 active_score 拆分为 behavior_active_score 与仅供诊断的 tag_health_score。
5. 统一客户经营 `intent_score + intent_level`，lead_grade 仅兼容。
6. 将表单、落地页、活动报名、资料留资等转化入口接入意向评分。
7. 新增企业 `account_score` 和企业下用户画像聚合。
8. 按一期 PRD 生成建议，采纳后创建经营动作，并实现跨页面去重与状态同步。
9. AI 创建分组接入现有用户分组。

## 7. 验收口径

```text
README 验收：标签画像底层能力是否可用
一期 PRD 验收：客户经营页面、评分、分层、推荐动作是否符合业务规则
```

禁止混用：

```text
不能在经营页面并列展示 lead_grade 和 intent_level 两套评级
不能用 README lifecycle 直接替代客户意向分层或作为业务阶段
不能让员工数据进入客户经营推荐
不能把标签分值直接当客户意向分
不能把事实标签计入行为活跃分
不能过滤表单、落地页、活动报名、资料留资等转化入口行为
```
