# 原型版本管理规范

## 1. 管理目标

本项目长期只维护一个正式在线地址：

<https://lucksomeone7-hue.github.io/yy/mk/>

正式地址始终由 `main` 分支自动发布。开发分支不会覆盖该地址；历史版本默认通过 Git Tag 和 GitHub Release 保存。只有跨期评审确实需要同时在线比较时，才单独生成临时或归档预览地址。

## 2. 版本号

采用语义化版本 `主版本.次版本.修订版本`：

| 变化 | 示例 | 使用场景 |
| --- | --- | --- |
| 主版本 Major | `1.6.2 → 2.0.0` | 信息架构、核心流程、视觉体系或业务阶段发生重大变化。 |
| 次版本 Minor | `1.6.2 → 1.7.0` | 新增模块、页面或完整业务能力。 |
| 修订版本 Patch | `1.6.2 → 1.6.3` | 文案、样式、数据示例或局部交互修正。 |

原型页面、`prototype/version.json`、`CHANGELOG.md`、Git Tag 和 GitHub Release 中的版本号必须一致。

## 3. 分支规则

| 分支 | 用途 | 发布行为 |
| --- | --- | --- |
| `main` | 当前稳定、可正式评审的版本 | 自动发布到 `/mk/`。 |
| `develop` | 下一次大版本或多功能并行开发的集成分支 | 不自动发布。小改动阶段可不使用。 |
| `codex/feature-xxx` | 单项需求、页面或交互调整 | 完成评审并合并后删除。 |
| `release/vX.Y.Z` | 大版本冻结、集中验收和修复 | 验收完成后合并到 `main`，不得继续加入新需求。 |
| `hotfix/vX.Y.Z` | 修复已发布版本的紧急问题 | 从 `main` 创建，完成后合并回 `main` 和 `develop`。 |

日常小改动从 `main` 创建功能分支；下一大版本已经进入并行开发时，从 `develop` 创建功能分支。不得直接在 `main` 上开发。

## 4. 标准流程

### 4.1 日常功能或修订

```text
main
  → codex/feature-xxx
  → 本地检查和评审
  → 合并 main
  → 更新 /mk/
  → 需要形成评审基线时创建 Tag
```

### 4.2 大版本

```text
main
  → develop
  → 多个 codex/feature-xxx 合并到 develop
  → release/vX.Y.Z 冻结验收
  → 更新版本文件和 CHANGELOG
  → 合并 main
  → 创建 vX.Y.Z Tag 和 GitHub Release
  → /mk/ 更新为新稳定版
```

### 4.3 紧急修复

```text
main
  → hotfix/vX.Y.Z
  → 验证修复
  → 合并 main 和 develop
  → 创建修订版本 Tag
```

## 5. 在线预览规则

1. `/mk/` 是唯一长期正式地址，只展示 `main`。
2. 功能分支和 `develop` 默认只做本地预览，不自动部署。
3. 阶段评审需要远程访问时，可为当前分支生成临时预览；评审结束后关闭。
4. 历史版本通过 Tag 回溯，不长期复制到 Pages 目录。
5. 只有两个大版本需要长期并行演示时，才建立 `/archive/vX/`；归档版本不再随代码提交更新。

## 6. 发布内容

正式发布前必须同步更新：

- `prototype/version.json`：当前版本、名称、日期和发布状态；
- 原型侧边栏版本标识；
- `CHANGELOG.md`：新增、调整、修复和已知限制；
- Git Tag：格式固定为 `vX.Y.Z`；
- GitHub Release：说明版本目标、主要变化、对应 PRD 或规范和已知限制。

Tag 是不可变的评审基线。Tag 已推送后不得移动或覆盖；需要修正时递增修订版本。

## 7. 提交规则

提交信息使用以下类型：

| 类型 | 用途 |
| --- | --- |
| `feat:` | 新页面、新流程或新能力。 |
| `fix:` | 功能或交互问题修复。 |
| `style:` | 不改变业务行为的视觉调整。 |
| `docs:` | PRD、规范和说明文档。 |
| `chore:` | 版本、构建和仓库维护。 |

一个提交只解决一个清晰问题。版本发布提交建议使用：`chore: release vX.Y.Z`。

## 8. 发布验收清单

- [ ] 页面主流程和本次变更已完成走查。
- [ ] JavaScript 语法检查及 `git diff --check` 通过。
- [ ] 1280、1440、1920px 关键页面布局可用。
- [ ] 原型版本号、`version.json` 和 `CHANGELOG.md` 一致。
- [ ] `release/vX.Y.Z` 验收期间没有混入新需求。
- [ ] 合并到 `main` 后 `/mk/` 可访问且版本标识正确。
- [ ] 已创建并推送 `vX.Y.Z` Tag；大版本已创建 GitHub Release。

